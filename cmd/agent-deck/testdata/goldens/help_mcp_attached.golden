Usage: agent-deck mcp attached [session-id] [options]

Show MCPs attached to a session.
If no session ID is provided, uses the current session (if in tmux).

Options:
  -json
    	Output as JSON
  -q	Minimal output (short)
  -quiet
    	Minimal output
